# Fonts

Display font is loaded from `public/fonts/`:

- `SVN-RadiantSlender.otf` → `@font-face` in `src/app/globals.css` as `"SVN-Radiant Slender"`

Montserrat is loaded via `next/font/google` in `src/app/layout.tsx`.
